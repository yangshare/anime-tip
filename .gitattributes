# 统一行尾：文本文件在仓库中统一为 LF，签出时按平台处理
* text=auto eol=lf

# Windows 专有二进制/脚本保持 CRLF
*.ps1   text eol=crlf
*.bat   text eol=crlf
*.cmd   text eol=crlf

# 二进制文件，不做行尾转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.db    binary
